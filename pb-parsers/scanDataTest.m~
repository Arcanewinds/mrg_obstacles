scanDat = load('../../bigData/scanData1.mat');
scanDat = scanDat.scanLog;
addpath('mrg');
figure(1)
for i = 1:1000
    scan = scanDat{i, 1};
    poles = poles_cluster(scan);
    subplot(1,2,1);
    ShowLaserScan(scan);
    viscircles([poles(:,1)
    %subplot(1,2,2);
    %ShowLaserScan(scan);
    drawnow
end